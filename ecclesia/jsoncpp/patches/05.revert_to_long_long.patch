diff --git a/google3/third_party/jsoncpp/config.h b/google3/third_party/jsoncpp/config.h
index 5a1e9b4..d2ff933 100644
--- a/google3/third_party/jsoncpp/config.h
+++ b/google3/third_party/jsoncpp/config.h
@@ -152,7 +152,10 @@ typedef unsigned int LargestUInt;
 typedef __int64 Int64;
 typedef unsigned __int64 UInt64;
 #else                 // if defined(_MSC_VER) // Other platforms, use long long
-// Google: keep long long for now, switching types breaks some callers.
+// TODO(codewiz): Reverting the type change for now to reduce collateral
+// damage across google3 when importing jsoncpp 1.7.6. We'll move to (u)int64_t
+// with a separate CL.
+//
 // typedef int64_t Int64;
 // typedef uint64_t UInt64;
 typedef long long int Int64;
